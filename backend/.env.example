SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOi...  # service_role secret
SUPABASE_ANON_KEY=eyJhbGciOi...      # anon public key

# Required stable server-only secret (at least 32 random characters).
# Set it in Railway Variables; never commit its real value.
# Changing the signing secret requires users to log in again.
AUTH_SESSION_SECRET=
